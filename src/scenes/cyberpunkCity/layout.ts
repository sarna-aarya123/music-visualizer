export const FOG_COLOR = '#05030c';

/** Fixed for now so the world is reproducible run to run — this is what a
 *  future "seed" input / Generate Visual / share link would plug into. */
export const WORLD_SEED = 1337;
